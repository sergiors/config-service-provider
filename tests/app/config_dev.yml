imports:
    - { resource: config.yml }

twig.options:
    debug: false

db.options:
      driver: pdo_pgsql
      host: ~
      user: ~
      password: ~
      dbname: ~

router:
    resource: %root_dir%/routing.yml

foo.options:
    foo:
        bar: beer
        beer:
            - 1
            - 2
